apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "minecraft.fullname" . }}-server-properties
  labels:
    {{- include "minecraft.labels" . | nindent 4 }}
data:
  server.properties: |
{{ include "minecraft.serverPropertiesContent" . | trim | indent 4 }}
